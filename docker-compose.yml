version: '3'

services:
  web:
    hostname: web
    build: .
    ports:
      - "8001:3000"
    command: bash -c "bundle exec rake db:migrate -e production"
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0' -e production"
